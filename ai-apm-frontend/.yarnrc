# Default: npm official registry (works for open-source contributors worldwide)
registry "https://registry.npmjs.org/"

# China mirror (uncomment if npmjs.org is slow or unreachable):
# registry "https://registry.npmmirror.com/"
